{
  "name": "@tangible/design",
  "type": "module",
  "version": "2024.09.18",
  "scripts": {
    "start": "roll dev",
    "build": "roll build && npm run docs:build",
    "format": "roll format",
    "version": "node version.js"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8"
  },
  "devDependencies": {
    "@tangible/roller": "2",
    "concurrently": "^8.2.2"
  }
}
